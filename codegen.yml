overwrite: true
schema: "./schema.graphql"
documents: "operation.graphql"
generates:
  src/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
